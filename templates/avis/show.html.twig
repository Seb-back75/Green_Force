{% extends 'base.html.twig' %}

{% block title %}Avis{% endblock %}

{% block body %}
    <h1>Avis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ avi.id }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ avi.Commentaire }}</td>
            </tr>
            <tr>
                <th>User</th>
                <td>{{ avi.User }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ avi.Date ? avi.Date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('avis_index') }}">Revenir à la liste</a>

    <a href="{{ path('avis_edit', {'id': avi.id}) }}">Modifier</a>

    {{ include('avis/_delete_form.html.twig') }}
{% endblock %}
