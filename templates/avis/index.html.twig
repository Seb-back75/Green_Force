{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
<div class="container-fluid">
   <div class="row justify-content-md-center">
       <div class="col-md-12 avv row justify-content-center"><div class="col-md-3 title"></div>
    </div>
</div>
    
    <div class="container-fluid">
    <div class="row col-md-12 justify-content-center gen">
    <div class="col-md-8 gen">
   <br> <h1>Avis</h1> 
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Commentaires</th>
                <th>User</th>
                <th>Date</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for avi in avis %}
            <tr>
                <td>{{ avi.id }}</td>
                <td>{{ avi.Commentaire }}</td>
                <td>{{ avi.User }}</td>
                <td>{{ avi.Date ? avi.Date|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('avis_show', {'id': avi.id}) }}">Voir</a>
                    <a href="{{ path('avis_edit', {'id': avi.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">Pas d'avis pour le moment</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('avis_new') }}">Créer un nouveau</a>
 </div>
    </div>
</div>



{% endblock %}

    

