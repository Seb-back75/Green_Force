{% extends 'base.html.twig' %}

{% block title %}Paiement{% endblock %}

{% block body %}
    <h1>Paiement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paiement.id }}</td>
            </tr>
            <tr>
                <th>Numero_CB</th>
                <td>{{ paiement.NumeroCB }}</td>
            </tr>
            <tr>
                <th>Mois</th>
                <td>{{ paiement.Mois }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ paiement.Annee }}</td>
            </tr>
            <tr>
                <th>Nom_Titulaire</th>
                <td>{{ paiement.NomTitulaire }}</td>
            </tr>
            <tr>
                <th>CVC</th>
                <td>{{ paiement.CVC }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('paiement_index') }}">back to list</a>

    <a href="{{ path('paiement_edit', {'id': paiement.id}) }}">edit</a>

    {{ include('paiement/_delete_form.html.twig') }}
{% endblock %}
