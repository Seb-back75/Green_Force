<form method="post" action="{{ path('papeterie_delete', {'id': papeterie.id}) }}" onsubmit="return confirm('Etes-vous sûre de vouloir supprimer cet article?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ papeterie.id) }}">
    <button class="btn"><p class="textf">Supprimer<p></button>
</form>
